{
  "version": 3,
  "builds": [
    {
      "src": "src/app.ts",
      "use": "@vercel/node",
      "config": { "zeroConfig": true }
    }
  ],
  "routes": [{ "src": "/(.*)", "dest": "src/app.ts" }]
}
